export class parametroEstudio {
    id: number = 0;
    descripcion: string = '';

    constructor(id: number, descripcion: string) {
        this.id = id;
        this.descripcion = descripcion;
    }
}